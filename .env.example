# Contraseña para la página /admin (si no se define, /admin queda abierta)
ADMIN_PASSWORD=cambia-esta-clave

# Token de Vercel Blob para guardar los resultados (se crea al conectar un Blob Store en Vercel)
BLOB_READ_WRITE_TOKEN=
